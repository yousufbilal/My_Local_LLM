# My Local LLM

Small local LangChain + Ollama project for experimenting with RAG over Pokemon and movie datasets.

## Structure

- `app/main.py`: chat loop
- `app/vector.py`: embeddings, vector stores, and retrievers
- `data/`: source CSV datasets

## Run

```bash
python -m app.main
```
